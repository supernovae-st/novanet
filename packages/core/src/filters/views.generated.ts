// Auto-generated by novanet v11.7.0 — DO NOT EDIT
// Source: packages/core/models/views/*.yaml
// Generated: 2026-02-11 16:13:01 UTC

// ─────────────────────────────────────────────────────────────────────────────
// Types
// ─────────────────────────────────────────────────────────────────────────────

export type Direction = 'outgoing' | 'incoming' | 'both';

/** Icon with web (Lucide) and terminal (Unicode) variants. */
export interface ViewIcon {
  /** Lucide icon name for web/Studio. */
  web: string;
  /** Unicode symbol for terminal/TUI. */
  terminal: string;
}

export interface IncludeRule {
  relation: string;
  direction: Direction;
  depth?: number;
  targetTypes?: string[];
  include?: IncludeRule[];
}

export interface ViewFilters {
  locale?: string;
  maxDepth?: number;
}

export interface ViewDefinition {
  id: string;
  name: string;
  description: string;
  version?: string;
  root: {
    type: string;
    key?: string;
  };
  include: IncludeRule[];
  filters?: ViewFilters;
  category?: string;
}

export interface ViewRegistryEntry {
  id: string;
  description: string;
  category: string;
  modes: string[];
  /** Icon with web (Lucide) and terminal (Unicode) variants. */
  icon?: ViewIcon;
  /** Color for the view (hex string). */
  color?: string;
  /** Cypher query template (parameterized). */
  cypher?: string;
  /** Whether this view is contextual (appears in node sidebar). */
  contextual?: boolean;
  /** Node types this view applies to (for contextual views). */
  applicableTypes?: string[];
  /** Parameters for the Cypher template. */
  params?: string[];
}

// ─────────────────────────────────────────────────────────────────────────────
// Generated Views (12 views)
// ─────────────────────────────────────────────────────────────────────────────

export const VIEWS: Record<string, ViewDefinition> = {
  'block-generation': {
    id: 'block-generation',
    name: 'Block Generation Context',
    description: 'Full context for sub-agent to generate a single block',
    version: '10.3.0',
    root: {
      type: 'Block',
    },
    include: [{"direction":"outgoing","relation":"HAS_PROMPT"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"HAS_RULES"}],"relation":"OF_TYPE"},{"direction":"outgoing","relation":"HAS_INSTRUCTION"},{"depth":2,"direction":"outgoing","include":[{"direction":"outgoing","relation":"HAS_CONTENT"},{"direction":"outgoing","relation":"SEMANTIC_LINK"}],"relation":"USES_ENTITY"},{"direction":"outgoing","relation":"HAS_GENERATED"}],
    filters: {
      locale: '$locale',
    },
  },
  'block-semantic-network': {
    id: 'block-semantic-network',
    name: 'Spreading Activation',
    description: 'Block with concepts and spreading activation (2 hops)',
    version: '8.0.0',
    root: {
      type: 'Block',
    },
    include: [{"direction":"outgoing","relation":"HAS_PROMPT"},{"direction":"outgoing","relation":"OF_TYPE"},{"depth":2,"direction":"outgoing","relation":"USES_ENTITY"}],
    filters: {
      locale: '$locale',
    },
  },
  'complete-graph': {
    id: 'complete-graph',
    name: 'Complete Graph',
    description: 'Full NovaNet graph with all nodes and relations',
    version: '11.3.0',
    root: {
      type: 'Project',
    },
    include: [{"direction":"outgoing","relation":"HAS_BRAND_IDENTITY"},{"direction":"outgoing","relation":"HAS_CONTENT"},{"direction":"outgoing","relation":"SUPPORTS_LOCALE"},{"direction":"outgoing","relation":"DEFAULT_LOCALE"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"HAS_BLOCK"},{"direction":"outgoing","relation":"HAS_PROMPT"},{"direction":"outgoing","relation":"HAS_GENERATED"},{"direction":"outgoing","relation":"LINKS_TO"},{"direction":"outgoing","relation":"SUBTOPIC_OF"}],"relation":"HAS_PAGE"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"HAS_CONTENT"},{"direction":"outgoing","relation":"BELONGS_TO"},{"direction":"outgoing","relation":"SEMANTIC_LINK"}],"relation":"HAS_ENTITY"}],
  },
  'entity-ecosystem': {
    id: 'entity-ecosystem',
    name: 'Entity Network',
    description: 'Entity with all related data (content, SEO, semantic links)',
    version: '10.3.0',
    root: {
      type: 'Entity',
    },
    include: [{"direction":"outgoing","relation":"HAS_CONTENT"},{"direction":"outgoing","relation":"EXPRESSES"},{"depth":2,"direction":"both","relation":"SEMANTIC_LINK"}],
    filters: {
      locale: '$locale',
    },
  },
  'locale-full-knowledge': {
    id: 'locale-full-knowledge',
    name: 'Complete locale knowledge',
    description: 'Complete locale knowledge (Identity, Voice, Culture, Market, Lexicon)',
    version: '11.5.0',
    root: {
      type: 'Locale',
    },
    include: [{"direction":"outgoing","relation":"HAS_CULTURE"},{"direction":"outgoing","relation":"HAS_MARKET"},{"direction":"outgoing","relation":"HAS_FORMATTING"},{"direction":"outgoing","relation":"HAS_SLUGIFICATION"},{"direction":"outgoing","relation":"HAS_EXPRESSIONS"}],
  },
  'page-generation-context': {
    id: 'page-generation-context',
    name: 'Page Generation Context',
    description: 'Full context for orchestrator to generate a page',
    version: '8.0.0',
    root: {
      type: 'Page',
    },
    include: [{"depth":1,"direction":"outgoing","relation":"HAS_BLOCK"},{"direction":"outgoing","relation":"HAS_PROMPT"},{"depth":2,"direction":"outgoing","relation":"USES_ENTITY"},{"direction":"outgoing","relation":"HAS_GENERATED"}],
    filters: {
      locale: '$locale',
    },
  },
  'project-context': {
    id: 'project-context',
    name: 'Project Context',
    description: 'Project boundaries, pages, and brand identity',
    version: '8.0.0',
    root: {
      type: 'Project',
    },
    include: [{"direction":"outgoing","relation":"HAS_BRAND_IDENTITY"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"FOR_LOCALE"}],"relation":"HAS_CONTENT"},{"direction":"outgoing","relation":"SUPPORTS_LOCALE"},{"direction":"outgoing","relation":"DEFAULT_LOCALE"},{"direction":"outgoing","relation":"HAS_PAGE"}],
  },
  'project-layer': {
    id: 'project-layer',
    name: 'Project Layer',
    description: 'Per-project nodes from foundation to generated output',
    version: '8.0.0',
    root: {
      type: 'Project',
    },
    include: [{"direction":"outgoing","relation":"HAS_BRAND_IDENTITY"},{"direction":"outgoing","relation":"HAS_CONTENT"},{"direction":"outgoing","relation":"SUPPORTS_LOCALE"},{"direction":"outgoing","relation":"DEFAULT_LOCALE"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"OF_TYPE"},{"direction":"outgoing","relation":"HAS_PROMPT"},{"direction":"outgoing","relation":"HAS_GENERATED"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"OF_TYPE"},{"direction":"outgoing","relation":"HAS_PROMPT"},{"direction":"outgoing","relation":"HAS_GENERATED"}],"relation":"HAS_BLOCK"}],"relation":"HAS_PAGE"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"HAS_CONTENT"},{"direction":"outgoing","relation":"SEMANTIC_LINK"},{"direction":"outgoing","relation":"BELONGS_TO"}],"relation":"HAS_ENTITY"}],
  },
  'project-overview': {
    id: 'project-overview',
    name: 'Project Overview',
    description: 'Project dashboard with pages, entities, and brand identity',
    version: '11.6',
    root: {
      type: 'Project',
    },
    include: [{"direction":"outgoing","relation":"HAS_PAGE"},{"direction":"outgoing","relation":"HAS_BRAND_IDENTITY"},{"direction":"outgoing","relation":"HAS_ENTITY"},{"direction":"outgoing","relation":"HAS_CONTENT"}],
  },
  'seo-keywords': {
    id: 'seo-keywords',
    name: 'SEO Keywords',
    description: 'SEO keywords and metrics (shared realm, per-locale)',
    version: '10.4.0',
    root: {
      type: 'Locale',
    },
    include: [{"direction":"incoming","include":[{"direction":"outgoing","relation":"HAS_METRICS"}],"relation":"FOR_LOCALE"},{"direction":"incoming","relation":"FOR_LOCALE"}],
  },
  'seo-pipeline': {
    id: 'seo-pipeline',
    name: 'SEO Pipeline',
    description: 'SEO keyword mining and internal linking workflow',
    version: '10.1.0',
    root: {
      type: 'Locale',
    },
    include: [{"direction":"incoming","include":[{"direction":"outgoing","relation":"HAS_METRICS"},{"direction":"incoming","relation":"EXPRESSES"}],"relation":"FOR_LOCALE"},{"direction":"incoming","relation":"FOR_LOCALE"}],
  },
  'shared-layer': {
    id: 'shared-layer',
    name: 'Shared Layer',
    description: 'Locale configuration and knowledge nodes shared across all organizations',
    version: '11.5.0',
    root: {
      type: 'Locale',
    },
    include: [{"direction":"outgoing","relation":"HAS_IDENTITY"},{"direction":"outgoing","relation":"HAS_VOICE"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"HAS_REFERENCE"}],"relation":"HAS_CULTURE"},{"direction":"outgoing","relation":"HAS_MARKET"},{"direction":"outgoing","include":[{"direction":"outgoing","relation":"HAS_EXPRESSION"},{"direction":"outgoing","relation":"HAS_METAPHOR"},{"direction":"outgoing","relation":"HAS_PATTERN"},{"direction":"outgoing","relation":"HAS_CONSTRAINT"}],"relation":"HAS_LEXICON"},{"direction":"outgoing","relation":"HAS_RULES_ADAPTATION"},{"direction":"outgoing","relation":"HAS_RULES_FORMATTING"},{"direction":"outgoing","relation":"HAS_RULES_SLUG"},{"direction":"outgoing","relation":"FALLBACK_TO"}],
  },
};

// ─────────────────────────────────────────────────────────────────────────────
// Generated Registry (29 entries)
// ─────────────────────────────────────────────────────────────────────────────

export const VIEW_REGISTRY: ViewRegistryEntry[] = [
  {
    id: 'meta-complete',
    description: 'Complete meta-graph (Realm, Layer, Kind, Trait, ArcKind)',
    category: 'meta',
    modes: ["meta"],
    icon: { web: 'diamond', terminal: '◆' },
    color: '#8b5cf6',
    cypher: `MATCH (n:Meta)
OPTIONAL MATCH (n)-[r]->(m:Meta)
RETURN n, r, m
`,
  },
  {
    id: 'meta-realm',
    description: 'Kinds of a specific realm with their layers',
    category: 'meta',
    modes: ["meta"],
    icon: { web: 'building-2', terminal: '◉' },
    color: '#6366f1',
    cypher: `MATCH (r:Meta:Realm {key: $realm})-[:HAS_LAYER]->(l:Meta:Layer)
OPTIONAL MATCH (l)-[:HAS_KIND]->(k:Meta:Kind)
OPTIONAL MATCH (k)-[:HAS_TRAIT]->(t:Meta:Trait)
RETURN r, l, k, t
`,
    params: ["realm"],
  },
  {
    id: 'meta-arcs',
    description: 'ArcKinds grouped by ArcFamily with source/target',
    category: 'meta',
    modes: ["meta"],
    icon: { web: 'git-branch', terminal: '→' },
    color: '#f59e0b',
    cypher: `MATCH (af:Meta:ArcFamily)<-[:BELONGS_TO_FAMILY]-(ak:Meta:ArcKind)
MATCH (ak)-[:FROM_KIND]->(source:Meta:Kind)
MATCH (ak)-[:TO_KIND]->(target:Meta:Kind)
RETURN af, ak, source, target
`,
  },
  {
    id: 'data-complete',
    description: 'All instance nodes (no meta)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'globe', terminal: '●' },
    color: '#6366f1',
    cypher: `MATCH (n) WHERE NOT n:Meta
OPTIONAL MATCH (n)-[r]->(m) WHERE NOT m:Meta
RETURN n, r, m
`,
  },
  {
    id: 'data-org',
    description: 'Organization realm nodes (6 layers, 21 nodes)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'building', terminal: '◎' },
    color: '#0ea5e9',
    cypher: `MATCH (n) WHERE NOT n:Meta
WITH n
MATCH (k:Meta:Kind {name: labels(n)[0]})
WHERE k.realm = 'org'
OPTIONAL MATCH (n)-[r]->(m) WHERE NOT m:Meta
RETURN n, r, m
`,
  },
  {
    id: 'data-shared',
    description: 'Shared realm nodes (4 layers, 39 nodes)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'earth', terminal: '◉' },
    color: '#2aa198',
    cypher: `MATCH (n) WHERE NOT n:Meta
WITH n
MATCH (k:Meta:Kind {name: labels(n)[0]})
WHERE k.realm = 'shared'
OPTIONAL MATCH (n)-[r]->(m) WHERE NOT m:Meta
RETURN n, r, m
`,
  },
  {
    id: 'data-geography',
    description: 'Geographic hierarchy (Continent → Region → Country)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'map', terminal: '▧' },
    color: '#14b8a6',
    cypher: `MATCH (c:Continent)
OPTIONAL MATCH (c)-[:HAS_REGION]->(r:GeoRegion)
OPTIONAL MATCH (r)-[:HAS_COUNTRY]->(co:Country)
OPTIONAL MATCH (co)-[:HAS_ZONE]->(z:GeoZone)
RETURN c, r, co, z
`,
  },
  {
    id: 'data-project',
    description: 'Project with all dependencies',
    category: 'data',
    modes: ["data"],
    icon: { web: 'folder-kanban', terminal: '▤' },
    color: '#6366f1',
    cypher: `MATCH (p:Project {key: $nodeKey})
OPTIONAL MATCH (p)-[:HAS_PAGE]->(page:Page)
OPTIONAL MATCH (p)-[:HAS_ENTITY]->(entity:Entity)
OPTIONAL MATCH (p)-[:HAS_BRAND]->(brand:BrandIdentity)
OPTIONAL MATCH (p)-[:HAS_CONTENT]->(content:ProjectContent)
OPTIONAL MATCH (content)-[:FOR_LOCALE]->(locale:Locale)
WITH p, collect(DISTINCT page) AS pages, collect(DISTINCT entity) AS entities,
     brand, collect(DISTINCT content) AS contents, collect(DISTINCT locale) AS locales
UNWIND ([p, brand] + pages + entities + contents + locales) AS n
WITH n WHERE n IS NOT NULL
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Project"],
  },
  {
    id: 'data-entities',
    description: 'Entities with content, categories, and keywords',
    category: 'data',
    modes: ["data"],
    icon: { web: 'link', terminal: '◈' },
    color: '#06b6d4',
    cypher: `MATCH (e:Entity)
OPTIONAL MATCH (e)-[:HAS_CONTENT]->(ec:EntityContent)
OPTIONAL MATCH (e)-[:BELONGS_TO]->(cat:EntityCategory)
OPTIONAL MATCH (ec)-[:FOR_LOCALE]->(locale:Locale)
OPTIONAL MATCH (e)-[:TARGETS]->(kw:SEOKeyword)
OPTIONAL MATCH (e)-[:MONITORS_GEO]->(gq:GEOQuery)
RETURN e, ec, cat, locale, kw, gq
`,
    contextual: true,
    applicableTypes: ["Entity","EntityCategory"],
  },
  {
    id: 'data-locale-coverage',
    description: 'Locale coverage by project (content per locale)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'languages', terminal: '◌' },
    color: '#f59e0b',
    cypher: `MATCH (l:Locale)
OPTIONAL MATCH (pc:ProjectContent)-[:FOR_LOCALE]->(l)
OPTIONAL MATCH (pg:PageGenerated)-[:FOR_LOCALE]->(l)
OPTIONAL MATCH (bg:BlockGenerated)-[:FOR_LOCALE]->(l)
OPTIONAL MATCH (ec:EntityContent)-[:FOR_LOCALE]->(l)
RETURN l, collect(DISTINCT pc) AS projectContents,
       collect(DISTINCT pg) AS pageGenerateds,
       collect(DISTINCT bg) AS blockGenerateds,
       collect(DISTINCT ec) AS entityContents
`,
    contextual: true,
    applicableTypes: ["Project","Locale"],
  },
  {
    id: 'data-seo',
    description: 'SEO intelligence (keywords, clusters, metrics)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'target', terminal: '◎' },
    color: '#8b5cf6',
    cypher: `MATCH (kw:SEOKeyword)
OPTIONAL MATCH (ks:SEOKeywordSet)-[:CONTAINS]->(kw)
OPTIONAL MATCH (kw)-[:HAS_METRICS]->(m:SEOKeywordMetrics)
OPTIONAL MATCH (kw)-[:IN_CLUSTER]->(c:SEOCluster)
OPTIONAL MATCH (e)-[:TARGETS]->(kw) WHERE e:Entity OR e:Page
RETURN kw, ks, m, c, collect(DISTINCT e) AS targeters
`,
    contextual: true,
    applicableTypes: ["SEOKeyword","SEOKeywordSet","SEOCluster"],
  },
  {
    id: 'data-geo',
    description: 'GEO intelligence (AI answers monitoring)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'radar', terminal: '◇' },
    color: '#a855f7',
    cypher: `MATCH (gq:GEOQuery)
OPTIONAL MATCH (gs:GEOQuerySet)-[:CONTAINS]->(gq)
OPTIONAL MATCH (gq)-[:HAS_ANSWER]->(ga:GEOAnswer)
OPTIONAL MATCH (ga)-[:HAS_METRICS]->(gm:GEOMetrics)
OPTIONAL MATCH (gq)-[:FOR_LOCALE]->(l:Locale)
OPTIONAL MATCH (e:Entity)-[:MONITORS_GEO]->(gq)
RETURN gq, gs, ga, gm, l, collect(DISTINCT e) AS monitors
`,
    contextual: true,
    applicableTypes: ["GEOQuery","GEOQuerySet","GEOAnswer"],
  },
  {
    id: 'data-generation',
    description: 'Generation pipeline (prompts → outputs)',
    category: 'data',
    modes: ["data"],
    icon: { web: 'zap', terminal: '★' },
    color: '#ec4899',
    cypher: `MATCH (p:Page)
OPTIONAL MATCH (pt:PageType)-[:TYPE_OF]->(p)
OPTIONAL MATCH (p)-[:HAS_PROMPT]->(pp:PagePrompt)
OPTIONAL MATCH (pp)<-[:COMPILED_FROM]-(pa:PromptArtifact)
OPTIONAL MATCH (p)-[:HAS_GENERATED]->(pg:PageGenerated)
OPTIONAL MATCH (pg)-[:FOR_LOCALE]->(l:Locale)
RETURN p, pt, pp, pa, pg, l
UNION
MATCH (b:Block)
OPTIONAL MATCH (bt:BlockType)-[:TYPE_OF]->(b)
OPTIONAL MATCH (b)-[:HAS_PROMPT]->(bp:BlockPrompt)
OPTIONAL MATCH (b)-[:HAS_GENERATED]->(bg:BlockGenerated)
OPTIONAL MATCH (bg)-[:FOR_LOCALE]->(l:Locale)
RETURN b AS p, bt AS pt, bp AS pp, null AS pa, bg AS pg, l
`,
    contextual: true,
    applicableTypes: ["Page","Block","PageGenerated","BlockGenerated"],
  },
  {
    id: 'overlay-complete',
    description: 'Meta + Data combined (debug architecture)',
    category: 'overlay',
    modes: ["overlay"],
    icon: { web: 'layers', terminal: '▣' },
    color: '#f97316',
    cypher: `MATCH (n)
OPTIONAL MATCH (n)-[r]->(m)
RETURN n, r, m
`,
  },
  {
    id: 'overlay-kind-instances',
    description: 'A Kind and all its instances',
    category: 'overlay',
    modes: ["overlay"],
    icon: { web: 'square-stack', terminal: '▤' },
    color: '#3b82f6',
    cypher: `MATCH (k:Meta:Kind {name: $kind})
OPTIONAL MATCH (n) WHERE $kind IN labels(n)
RETURN k, collect(DISTINCT n) AS instances
`,
    params: ["kind"],
  },
  {
    id: 'overlay-arc-analysis',
    description: 'ArcKind with instance examples',
    category: 'overlay',
    modes: ["overlay"],
    icon: { web: 'git-compare', terminal: '⇄' },
    color: '#f59e0b',
    cypher: `MATCH (ak:Meta:ArcKind {name: $arcKind})
MATCH (ak)-[:FROM_KIND]->(source:Meta:Kind)
MATCH (ak)-[:TO_KIND]->(target:Meta:Kind)
OPTIONAL MATCH (s)-[r]->(t)
WHERE type(r) = $arcKind
RETURN ak, source, target, collect({source: s, rel: r, target: t})[0..5] AS examples
`,
    params: ["arcKind"],
  },
  {
    id: 'ctx-composition',
    description: 'Page/Block composition hierarchy',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'box', terminal: '□' },
    color: '#0ea5e9',
    cypher: `MATCH (root {key: $nodeKey})
WHERE root:Page OR root:Block OR root:Project
OPTIONAL MATCH path = (root)-[:HAS_BLOCK|HAS_PAGE*1..3]->(child)
WITH root, collect(DISTINCT child) AS children
UNWIND ([root] + children) AS n
WITH collect(DISTINCT n) AS nodes
UNWIND nodes AS n
OPTIONAL MATCH (n)-[r:HAS_BLOCK|HAS_PAGE]->(m)
WHERE m IN nodes
RETURN nodes, collect(DISTINCT r) AS relationships
`,
    contextual: true,
    applicableTypes: ["Project","Page","Block"],
  },
  {
    id: 'ctx-entities',
    description: 'Connected entities (semantic links)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'link-2', terminal: '◈' },
    color: '#06b6d4',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)-[:USES_ENTITY|TARGETS*1..2]->(e:Entity)
OPTIONAL MATCH (e)-[:BELONGS_TO]->(c:EntityCategory)
WITH root, collect(DISTINCT e) AS entities, collect(DISTINCT c) AS categories
UNWIND ([root] + entities + categories) AS n
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Page","Block","Entity"],
  },
  {
    id: 'ctx-locales',
    description: 'Locale coverage (content per language)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'globe-2', terminal: '◌' },
    color: '#f59e0b',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)-[:HAS_CONTENT|HAS_GENERATED]->(content)-[:FOR_LOCALE]->(locale:Locale)
WITH root, collect(DISTINCT content) AS contents, collect(DISTINCT locale) AS locales
UNWIND ([root] + contents + locales) AS n
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Project","Page","Entity","Block"],
  },
  {
    id: 'ctx-knowledge',
    description: 'Knowledge atoms (Terms, Expressions, Patterns)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'brain', terminal: '◇' },
    color: '#22c55e',
    cypher: `MATCH (locale:Locale {key: $nodeKey})
OPTIONAL MATCH (locale)-[:HAS_TERMS|HAS_EXPRESSIONS|HAS_PATTERNS|HAS_CULTURE|HAS_TABOOS]->(set)
OPTIONAL MATCH (set)-[:CONTAINS_TERM|CONTAINS_EXPRESSION|CONTAINS_PATTERN|CONTAINS_CULTURE_REF|CONTAINS_TABOO]->(atom)
WITH locale, collect(DISTINCT set) AS sets, collect(DISTINCT atom) AS atoms
UNWIND ([locale] + sets + atoms) AS n
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Locale"],
  },
  {
    id: 'ctx-geographic',
    description: 'Geographic hierarchy (Continent → Region → Country)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'map-pin', terminal: '▧' },
    color: '#14b8a6',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)-[:FOR_COUNTRY]->(country:Country)
OPTIONAL MATCH (country)-[:IN_REGION]->(region)
OPTIONAL MATCH (region)-[:IN_CONTINENT]->(continent:Continent)
WITH root, country, region, continent
UNWIND [root, country, region, continent] AS n
WITH n WHERE n IS NOT NULL
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Locale","Continent","GeoRegion","Country"],
  },
  {
    id: 'ctx-generation',
    description: 'Generation pipeline (prompts → outputs)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'sparkles', terminal: '★' },
    color: '#ec4899',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)<-[:TYPE_OF]-(pageType:PageType)
OPTIONAL MATCH (root)-[:HAS_GENERATED]->(generated)
OPTIONAL MATCH (generated)-[:FOR_LOCALE]->(locale:Locale)
OPTIONAL MATCH (root)-[:HAS_PROMPT]->(prompt)
OPTIONAL MATCH (prompt)<-[:COMPILED_FROM]-(artifact:PromptArtifact)
WITH root, pageType, prompt, artifact, collect(DISTINCT generated) AS outputs, collect(DISTINCT locale) AS locales
UNWIND ([root, pageType, prompt, artifact] + outputs + locales) AS n
WITH n WHERE n IS NOT NULL
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Page","Block","PageGenerated","BlockGenerated","PagePrompt","BlockPrompt"],
  },
  {
    id: 'ctx-categories',
    description: 'Entity category classification',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'tag', terminal: '#' },
    color: '#64748b',
    cypher: `MATCH (root {key: $nodeKey})
WHERE root:Entity OR root:EntityCategory
OPTIONAL MATCH (root)-[:BELONGS_TO]->(category:EntityCategory)
OPTIONAL MATCH (root)<-[:BELONGS_TO]-(entity:Entity)
WITH root, category, collect(DISTINCT entity) AS entities
UNWIND ([root, category] + entities) AS n
WITH n WHERE n IS NOT NULL
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Entity","EntityCategory"],
  },
  {
    id: 'ctx-seo-intel',
    description: 'SEO keywords and clusters',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'search', terminal: '◎' },
    color: '#8b5cf6',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)-[:TARGETS]->(keyword:SEOKeyword)
OPTIONAL MATCH (keyword)-[:HAS_METRICS]->(metrics:SEOKeywordMetrics)
OPTIONAL MATCH (keyword)-[:IN_CLUSTER]->(cluster:SEOCluster)
WITH root, collect(DISTINCT keyword) AS keywords, collect(DISTINCT metrics) AS allMetrics, collect(DISTINCT cluster) AS clusters
UNWIND ([root] + keywords + allMetrics + clusters) AS n
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Entity","Page","SEOKeyword","SEOKeywordSet"],
  },
  {
    id: 'ctx-geo-intel',
    description: 'GEO queries and AI answers',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'radar', terminal: '◇' },
    color: '#a855f7',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)-[:MONITORS_GEO]->(query:GEOQuery)
OPTIONAL MATCH (query)-[:HAS_ANSWER]->(answer:GEOAnswer)
OPTIONAL MATCH (query)-[:IN_QUERY_SET]->(querySet:GEOQuerySet)
WITH root, collect(DISTINCT query) AS queries, collect(DISTINCT answer) AS answers, collect(DISTINCT querySet) AS querySets
UNWIND ([root] + queries + answers + querySets) AS n
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Entity","GEOQuery","GEOQuerySet","GEOAnswer"],
  },
  {
    id: 'ctx-project',
    description: 'Complete project structure (Pages, Entities, Brand)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'folder-open', terminal: '▤' },
    color: '#6366f1',
    cypher: `MATCH (project:Project {key: $nodeKey})
OPTIONAL MATCH (project)-[:HAS_PAGE]->(page:Page)
OPTIONAL MATCH (project)-[:HAS_ENTITY]->(entity:Entity)
OPTIONAL MATCH (project)-[:HAS_BRAND]->(brand:BrandIdentity)
WITH project, collect(DISTINCT page) AS pages, collect(DISTINCT entity) AS entities, brand
UNWIND ([project, brand] + pages + entities) AS n
WITH n WHERE n IS NOT NULL
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
    applicableTypes: ["Project"],
  },
  {
    id: 'ctx-arc-relationships',
    description: 'Arc relationships grouped by family',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'git-fork', terminal: '⇢' },
    color: '#f59e0b',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)-[r]->(target)
OPTIONAL MATCH (source)-[r2]->(root)
WITH root, collect({type: type(r), target: target, direction: 'outgoing'}) AS outgoing,
     collect({type: type(r2), source: source, direction: 'incoming'}) AS incoming
RETURN root, outgoing, incoming
`,
    contextual: true,
  },
  {
    id: 'ctx-full-context',
    description: 'Complete node context (all direct relationships)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'maximize-2', terminal: '⊕' },
    color: '#6366f1',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH (root)-[r_out]->(target)
OPTIONAL MATCH (source)-[r_in]->(root)
WITH root,
     collect(DISTINCT {rel: type(r_out), node: target}) AS outgoing,
     collect(DISTINCT {rel: type(r_in), node: source}) AS incoming
RETURN root, outgoing, incoming
`,
    contextual: true,
  },
  {
    id: 'ctx-neighborhood',
    description: 'Extended neighborhood (2 hops)',
    category: 'contextual',
    modes: ["data","overlay"],
    icon: { web: 'scan', terminal: '◎' },
    color: '#22c55e',
    cypher: `MATCH (root {key: $nodeKey})
OPTIONAL MATCH path = (root)-[*1..2]-(neighbor)
WITH root, collect(DISTINCT neighbor) AS neighbors, collect(DISTINCT relationships(path)) AS rels
UNWIND ([root] + neighbors) AS n
RETURN collect(DISTINCT n) AS nodes
`,
    contextual: true,
  },
];

// ─────────────────────────────────────────────────────────────────────────────
// Helpers
// ─────────────────────────────────────────────────────────────────────────────

/** Get a view by ID (throws if not found) */
export function getView(id: string): ViewDefinition {
  const view = VIEWS[id];
  if (!view) {
    throw new Error(`View '${id}' not found. Available: ${Object.keys(VIEWS).join(', ')}`);
  }
  return view;
}

/** Get all views in a category */
export function getViewsByCategory(category: string): ViewDefinition[] {
  return Object.values(VIEWS).filter(v => v.category === category);
}

/** Get registry entries for a navigation mode */
export function getViewsForMode(mode: string): ViewRegistryEntry[] {
  return VIEW_REGISTRY.filter(e => e.modes.includes(mode));
}

/** All view IDs */
export const VIEW_IDS = Object.keys(VIEWS) as readonly string[];

/** All category names */
export const VIEW_CATEGORIES = [...new Set(VIEW_REGISTRY.map(e => e.category))] as const;