# models/views/project-overview.yaml
id: project-overview
name: Project Overview
description: Project dashboard with pages and brand identity
version: "1.2"

root:
  type: Project

include:
  - relation: HAS_PAGE
    direction: outgoing

  - relation: HAS_BRAND_IDENTITY
    direction: outgoing

  # v10.3: HAS_CONCEPT removed — Entity in org realm, use USES_ENTITY from Page/Block

