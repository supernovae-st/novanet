# packages/core/models/arc-kinds/localization/output-of.yaml
# ArcKind: OUTPUT_OF — Inverse of HAS_OUTPUT — generated content points to structure

arc:
  name: OUTPUT_OF
  family: localization
  scope: intra_realm
  source:
    - PageL10n
    - BlockL10n
  target:
    - Page
    - Block
  cardinality: many_to_one
  inverse_of: HAS_OUTPUT
  llm_context: Inverse of HAS_OUTPUT — generated content points to structure.
  cypher_pattern: (PageL10n)-[:OUTPUT_OF]->(Page)
