# packages/core/models/arc-kinds/localization/has-identity.yaml
# ArcKind: HAS_IDENTITY — Locale's basic identity: language name, native endonym, script info

arc:
  name: HAS_IDENTITY
  family: localization
  scope: intra_realm
  source: Locale
  target: LocaleIdentity
  cardinality: one_to_one
  llm_context: "Locale's basic identity: language name, native endonym, script info."
  cypher_pattern: (Locale)-[:HAS_IDENTITY]->(LocaleIdentity)
