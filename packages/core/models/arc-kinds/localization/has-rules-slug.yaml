# packages/core/models/arc-kinds/localization/has-rules-slug.yaml
# ArcKind: HAS_RULES_SLUG — URL slug rules: character transliteration, word separators

arc:
  name: HAS_RULES_SLUG
  family: localization
  scope: intra_realm
  source: Locale
  target: LocaleRulesSlug
  cardinality: one_to_one
  llm_context: "URL slug rules: character transliteration, word separators."
  cypher_pattern: (Locale)-[:HAS_RULES_SLUG]->(LocaleRulesSlug)
