# packages/core/models/arc-kinds/localization/has-pattern.yaml
# ArcKind: HAS_PATTERN — Formatting pattern: regex/template for dates, numbers, addresses

arc:
  name: HAS_PATTERN
  family: localization
  scope: intra_realm
  source: LocaleRulesFormatting
  target: Pattern
  cardinality: one_to_many
  llm_context: "Formatting pattern: regex/template for dates, numbers, addresses."
  cypher_pattern: (LocaleRulesFormatting)-[:HAS_PATTERN]->(Pattern)
