# packages/core/models/arc-kinds/ownership/supports-locale.yaml
# ArcKind: SUPPORTS_LOCALE — Project supports locales with activation status (active/pending/disabled)

arc:
  name: SUPPORTS_LOCALE
  family: ownership
  scope: intra_realm
  source: Project
  target: Locale
  cardinality: many_to_many
  properties:
    - name: status
      type: string
      required: false
  llm_context: Project supports locales with activation status (active/pending/disabled).
  cypher_pattern: (Project)-[:SUPPORTS_LOCALE {status}]->(Locale)
