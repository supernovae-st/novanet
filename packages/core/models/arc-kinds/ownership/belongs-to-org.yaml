# packages/core/models/arc-kinds/ownership/belongs-to-org.yaml
# ArcKind: BELONGS_TO_ORG — Project belongs to OrgConfig (v11.3: unified)

arc:
  name: BELONGS_TO_ORG
  family: ownership
  scope: intra_realm
  source: Project
  target: OrgConfig
  cardinality: many_to_one
  llm_context: Project belongs to organization. Use to find org-level context for a project.
  cypher_pattern: (Project)-[:BELONGS_TO_ORG]->(OrgConfig)
