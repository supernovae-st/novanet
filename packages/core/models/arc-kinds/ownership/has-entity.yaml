# packages/core/models/arc-kinds/ownership/has-entity.yaml
# ArcKind: HAS_ENTITY — Organization/Project owns Entity nodes (v10.5)

arc:
  name: HAS_ENTITY
  family: ownership
  scope: intra_realm
  source: Organization
  target: Entity
  cardinality: one_to_many
  llm_context: Organization or Project owns entities. Use to get semantic concepts for context.
  cypher_pattern: (Organization)-[:HAS_ENTITY]->(Entity)
