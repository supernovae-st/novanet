# packages/core/models/arc-kinds/ownership/pillar-page.yaml
# ArcKind: PILLAR_PAGE — TopicCluster has ONE pillar page as authoritative hub

arc:
  name: PILLAR_PAGE
  family: ownership
  scope: intra_realm
  source: TopicCluster
  target: Page
  cardinality: one_to_one
  llm_context: TopicCluster has ONE pillar page as authoritative hub. All cluster pages link to it.
  cypher_pattern: (TopicCluster)-[:PILLAR_PAGE]->(Page)
