# packages/core/models/arc-kinds/ownership/has-project.yaml
# ArcKind: HAS_PROJECT — OrgConfig owns projects (v11.3: unified from Tenant+Organization)

arc:
  name: HAS_PROJECT
  family: ownership
  scope: intra_realm
  source: OrgConfig
  target: Project
  cardinality: one_to_many
  llm_context: OrgConfig owns all projects. Use to enumerate all projects for an organization.
  cypher_pattern: (OrgConfig)-[:HAS_PROJECT]->(Project)
