# packages/core/models/arc-kinds/ownership/has-project.yaml
# ArcKind: HAS_PROJECT — Tenant owns projects (v10.6: Organization renamed to Tenant)

arc:
  name: HAS_PROJECT
  family: ownership
  scope: intra_realm
  source: Tenant
  target: Project
  cardinality: one_to_many
  llm_context: Tenant owns product projects. Use to enumerate all projects for a tenant.
  cypher_pattern: (Tenant)-[:HAS_PROJECT]->(Project)
