# packages/core/models/arc-kinds/ownership/of-type.yaml
# ArcKind: OF_TYPE — Block references its type template (v11.8)
#
# v11.8 (ADR-025):
#   - Page→PageType REMOVED (use HAS_STRUCTURE for Page→PageStructure)
#   - Block→BlockType RETAINED

arc:
  name: OF_TYPE
  family: ownership
  scope: intra_realm
  source: Block
  target: BlockType
  cardinality: many_to_one
  llm_context: "Block references its type template defining JSON schema."
  cypher_pattern: (Block)-[:OF_TYPE]->(BlockType)
