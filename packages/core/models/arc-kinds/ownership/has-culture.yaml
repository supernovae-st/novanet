# packages/core/models/arc-kinds/ownership/has-culture.yaml
# v10.7: Updated to link Locale -> Culture config node (1:1)
# Note: For CultureSet (knowledge atoms), use HAS_CULTURE_SET arc
arc:
  name: HAS_CULTURE
  family: ownership
  scope: intra_realm
  source: Locale
  target: Culture
  cardinality: one_to_one
  llm_context: Every locale has exactly one culture configuration node.
  cypher_pattern: (Locale)-[:HAS_CULTURE]->(Culture)
