# packages/core/models/arc-kinds/semantic/semantic-link.yaml
# ArcKind: SEMANTIC_LINK — Spreading activation links between concepts

arc:
  name: SEMANTIC_LINK
  family: semantic
  scope: intra_realm
  source: Concept
  target: Concept
  cardinality: many_to_many
  is_self_referential: true
  temperature_threshold: 0.3  # v9.9: Min temperature for traversal (high-confidence links)
  properties:
    - name: type
      type: string
      required: false
    - name: temperature
      type: string
      required: false
  llm_context: "Spreading activation links between concepts. temperature: 0.0-1.0, cutoff 0.3, max 2 hops."
  cypher_pattern: (Concept)-[:SEMANTIC_LINK {type, temperature}]->(Concept)
