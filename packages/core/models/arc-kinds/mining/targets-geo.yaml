# packages/core/models/arc-kinds/mining/targets-geo.yaml
# ArcKind: TARGETS_GEO — Cross-locale GEO shortcut for reporting

arc:
  name: TARGETS_GEO
  family: mining
  scope: intra_realm
  source: Concept
  target: GEOSeedL10n
  cardinality: one_to_many
  properties:
    - name: status
      type: string
      required: false
    - name: priority
      type: string
      required: false
  llm_context: "Cross-locale GEO shortcut for reporting. status: active/monitoring/archived."
  cypher_pattern: (Concept)-[:TARGETS_GEO {status, priority}]->(GEOSeedL10n)
