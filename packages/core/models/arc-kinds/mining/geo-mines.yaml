# packages/core/models/arc-kinds/mining/geo-mines.yaml
# ArcKind: GEO_MINES — GEO mining run produces seed query data

arc:
  name: GEO_MINES
  family: mining
  scope: intra_realm
  source: GEOMiningRun
  target: GEOSeedL10n
  cardinality: one_to_many
  llm_context: GEO mining run produces seed query data.
  cypher_pattern: (GEOMiningRun)-[:GEO_MINES]->(GEOSeedL10n)
