# packages/core/models/arc-kinds/generation/influenced-by.yaml
# ArcKind: INFLUENCED_BY — Provenance: which concepts influenced this output

arc:
  name: INFLUENCED_BY
  family: generation
  scope: intra_realm
  source: BlockL10n
  target: ConceptL10n
  cardinality: many_to_many
  properties:
    - name: weight
      type: string
      required: false
    - name: concept_version
      type: string
      required: false
  llm_context: "Provenance: which concepts influenced this output. weight: 0.0-1.0."
  cypher_pattern: (BlockL10n)-[:INFLUENCED_BY {weight, concept_version}]->(ConceptL10n)
