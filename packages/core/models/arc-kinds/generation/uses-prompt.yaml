# packages/core/models/arc-kinds/generation/uses-prompt.yaml
# ArcKind: USES_PROMPT — Generation job uses compiled prompt artifacts for generation

arc:
  name: USES_PROMPT
  family: generation
  scope: intra_realm
  source: GenerationJob
  target: PromptArtifact
  cardinality: many_to_many
  llm_context: Generation job uses compiled prompt artifacts for generation.
  cypher_pattern: (GenerationJob)-[:USES_PROMPT]->(PromptArtifact)
