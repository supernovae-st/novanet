# packages/core/models/arc-kinds/generation/includes-entity.yaml
# ArcKind: INCLUDES_ENTITY — Prompt artifact includes entity context for generation

arc:
  name: INCLUDES_ENTITY
  family: generation
  scope: cross_realm
  source: PromptArtifact
  target: Entity
  cardinality: one_to_many
  properties:
    - name: weight
      type: string
      required: false
  llm_context: "Prompt artifact includes entity context for generation. weight: 0.0-1.0 influence on output."
  cypher_pattern: (PromptArtifact)-[:INCLUDES_ENTITY {weight}]->(Entity)
