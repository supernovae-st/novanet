# packages/core/models/arc-kinds/generation/evaluated-by-job.yaml
# ArcKind: EVALUATED_BY_JOB — Evaluation signal relates to generation job that produced evaluated content

arc:
  name: EVALUATED_BY_JOB
  family: generation
  scope: intra_realm
  source: EvaluationSignal
  target: GenerationJob
  cardinality: many_to_one
  llm_context: Evaluation signal relates to generation job that produced evaluated content.
  cypher_pattern: (EvaluationSignal)-[:EVALUATED_BY_JOB]->(GenerationJob)
