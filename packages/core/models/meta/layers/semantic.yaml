# models/meta/layers/semantic.yaml
# NodeLayer: Semantic — Meaning and concepts

key: semantic
display_name: Semantic Layer
emoji: "💡"
color: "#f97316"
realm: tenant

llm_context: |
  Meaning and semantics. For tenant realm: Entity, EntityContent, AudiencePersona,
  and ChannelSurface. Entity is the invariant semantic building block, EntityContent
  is its locale-specific content. Entities link via SEMANTIC_LINK for spreading activation.
