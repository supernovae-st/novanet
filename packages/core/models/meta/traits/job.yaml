# models/meta/traits/job.yaml
# NodeTrait: Job — Background processing tasks

key: job
display_name: Job
color: "#6b7280"

visual_encoding:
  border_style: none
  border_width: 0
  unicode_border: " "

llm_context: |
  Background processing tasks. Mining runs, batch operations, and other
  asynchronous jobs that produce or update data nodes.
