# models/meta/arc-families/generation.yaml
# ArcFamily: Generation — LLM generation pipeline flow

key: generation
display_name: Generation
color: "#8b5cf6"

visual_encoding:
  stroke_style: solid
  stroke_width: 3
  arrow_style: "==>"

retrieval:
  default_traversal: lazy  # Follow for provenance if budget allows

llm_context: |
  LLM generation pipeline flow. Provenance tracking from prompts to
  outputs, version history, and assembly relationships.
