# models/meta/arc-families/semantic.yaml
# ArcFamily: Semantic — Meaning and concept connections

key: semantic
display_name: Semantic
color: "#f97316"

visual_encoding:
  stroke_style: dotted
  stroke_width: 2
  arrow_style: ".->"

retrieval:
  default_traversal: lazy  # Follow if budget allows, temperature-gated

llm_context: |
  Meaning and concept connections. Spreading activation traversal with
  temperature-based filtering. Links concepts to each other and to
  the pages/blocks that use them.
