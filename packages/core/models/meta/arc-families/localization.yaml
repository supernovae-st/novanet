# models/meta/arc-families/localization.yaml
# ArcFamily: Localization — Invariant to locale-specific links

key: localization
display_name: Localization
color: "#22c55e"

visual_encoding:
  stroke_style: dashed
  stroke_width: 2
  arrow_style: ".->"

retrieval:
  default_traversal: eager  # Always follow for content localization

llm_context: |
  Links between invariant nodes and their locale-specific content.
  Includes locale knowledge edges (Locale -> LocaleVoice, etc.) and
  content localization edges (Entity -> EntityContent, etc.).
