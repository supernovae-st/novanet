# models/meta/arc-families/ownership.yaml
# ArcFamily: Ownership — Parent-child structural relationships

key: ownership
display_name: Ownership
color: "#3b82f6"

visual_encoding:
  stroke_style: solid
  stroke_width: 2
  arrow_style: "-->"

retrieval:
  default_traversal: eager  # Always follow in context assembly

llm_context: |
  Parent-child structural relationships. A node "owns" or "contains"
  another. Always follow in traversal (structural backbone).
