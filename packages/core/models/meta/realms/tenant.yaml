# models/meta/realms/tenant.yaml
# NodeRealm: Tenant — Business-specific nodes (v10.6: merged organization + project)

key: tenant
display_name: Tenant
emoji: "🏢"
color: "#6c71c4"

layers:
  - config
  - foundation
  - structure
  - semantic
  - instruction
  - output

llm_context: |
  Business-specific nodes for a tenant (organization + projects). Contains
  organization config, brand identity, page structure, semantic entities,
  generation prompts, and localized outputs. v10.6 merged organization and
  project realms into single TENANT realm for simpler multi-tenant isolation.
