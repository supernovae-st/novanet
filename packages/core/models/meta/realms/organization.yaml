# models/meta/realms/organization.yaml
# NodeRealm: Organization — Multi-tenant company-level nodes (NEW in v10.5)

key: organization
display_name: Organization
emoji: "🏢"
color: "#d33682"

layers:
  - config
  - semantic

llm_context: |
  Organization-level nodes for multi-tenant isolation. Contains company
  configuration, shared Entity definitions, and EntityL10n that apply
  across all projects within an organization. Enables brand consistency
  while allowing project-specific customization. NEW in v10.5.
