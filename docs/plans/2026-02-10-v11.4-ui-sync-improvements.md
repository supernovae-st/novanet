# v11.4 UI & Taxonomy Sync Improvements

**Date**: 2026-02-10
**Status**: In Progress
**Version**: v11.4.0

## Context

After updating SchemaNode/MetaBadgeNode with premium gradient backgrounds and enlarged badges, we identified several sync issues between the Studio UI and the YAML taxonomy source of truth.

## Completed Tasks

- [x] Add gradient background behind icon (SchemaNode, MetaBadgeNode)
- [x] Enlarge classification tag badges (8px → 11px)
- [x] Remove obsolete `seo` and `geo` layers from Layer type
- [x] Update SEO/GEO nodes to `shared/knowledge` realm/layer
- [x] Update KIND_META classifications

## Remaining Tasks

### Task 1: Add shared/config layer to UI facets
**Files**: `apps/studio/src/config/nodeTypes.ts`
- Add `config` layer to NODE_VISUAL_LAYERS for shared realm
- EntityCategory node needs to be visible in facet filtering

### Task 2: Centralize color definitions
**Files**:
- `packages/core/src/graph/colors.generated.ts` (create if missing)
- `apps/studio/src/components/graph/schema/SchemaNode.tsx`
- `apps/studio/src/components/graph/schema/MetaBadgeNode.tsx`
- Import colors from generated file instead of hardcoding

### Task 3: Run schema regeneration
**Command**: `cargo run -- schema generate`
- Regenerate all 12 artifacts from YAML
- Ensure Cypher/Mermaid match TypeScript changes

### Task 4: Type-check and verify
**Commands**:
- `pnpm type-check` - Verify no type errors
- `pnpm lint` - Verify no lint errors

## Architecture After Changes

```
v11.4 Layer Structure (10 layers total):
├── SHARED (4 layers, 40 nodes)
│   ├── config (1): EntityCategory
│   ├── locale (7): Locale, Formatting, etc.
│   ├── geography (6): Continent, GeoRegion, etc.
│   └── knowledge (26): TermSet, SEOKeyword, GEOQuery, etc.
│
└── ORG (6 layers, 21 nodes)
    ├── config (1): OrgConfig
    ├── foundation (3): Project, Brand, ProjectContent
    ├── structure (3): Page, Block, ContentSlot
    ├── semantic (4): Entity, EntityContent, etc.
    ├── instruction (7): PagePrompt, BlockPrompt, etc.
    └── output (3): PageGenerated, BlockGenerated, OutputArtifact
```

## Success Criteria

1. All 61 node types correctly classified by realm/layer/trait
2. UI facets show correct layer groupings
3. No hardcoded colors in node components
4. `pnpm type-check` passes
5. `cargo run -- schema validate` passes
