# v11.4/v11.5 Implementation Plan — SEO/GEO + Layer Clarification

**Date**: 2026-02-10
**Spec**: `2026-02-10-seo-geo-shared-knowledge-v11.4-spec.md`
**Status**: v11.5.1 Scope Fixes DONE ✅ | v11.4 SEO/GEO Phases 2-7 Ready

---

## v11.5.1 Arc Scope Fixes (COMPLETED)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  v11.5.1 CHANGES (DONE)                                                         │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  5 ARC SCOPES FIXED after SEO/GEO → shared/knowledge move:                      │
│                                                                                 │
│  CROSS-REALM (org ↔ shared):                                                    │
│  ├── ✅ TARGETS: intra_realm → cross_realm                                      │
│  │   └── EntityContent (org/semantic) → SEOKeyword (shared/knowledge)          │
│  ├── ✅ MONITORS_GEO: intra_realm → cross_realm                                 │
│  │   └── EntityContent (org/semantic) → GEOQuery (shared/knowledge)            │
│  └── ✅ EXPRESSES: intra_realm → cross_realm                                    │
│      └── SEOKeyword (shared/knowledge) → Entity (org/semantic)                  │
│                                                                                 │
│  INTRA-REALM (shared → shared):                                                 │
│  ├── ✅ HAS_SEO_KEYWORDS: cross_realm → intra_realm                             │
│  │   └── Locale (shared/config) → SEOKeyword (shared/knowledge)                 │
│  └── ✅ HAS_GEO_QUERIES: cross_realm → intra_realm                              │
│      └── Locale (shared/config) → GEOQuery (shared/knowledge)                   │
│                                                                                 │
│  VALIDATION: cargo run -- schema validate → 0 errors, 0 warnings               │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## v11.5 Layer Clarification (COMPLETED)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  v11.5 CHANGES (DONE)                                                           │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ✅ MOVE: Locale from shared/locale → shared/config                             │
│                                                                                 │
│  RATIONALE:                                                                     │
│  ├── Locale is a DEFINITION (invariant) - fr-FR is fr-FR everywhere            │
│  ├── Culture, Style, etc. are PARAMETERS (knowledge) - different per locale    │
│  └── Follows EntityCategory pattern: definitions in config layer               │
│                                                                                 │
│  RULE ESTABLISHED:                                                              │
│  ├── shared/config  = invariant trait  (Locale, EntityCategory, SEOKeywordFormat)│
│  └── shared/locale  = knowledge trait  (Culture, Style, Formatting, etc.)       │
│                                                                                 │
│  FILES CHANGED:                                                                 │
│  ├── node-kinds/shared/locale/locale.yaml → node-kinds/shared/config/locale.yaml│
│  └── taxonomy.yaml (version 11.4.0 → 11.5.0, layer descriptions updated)        │
│                                                                                 │
│  VALIDATION: cargo run -- schema validate → 0 errors, 0 warnings               │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## v11.4 SEO/GEO Migration Overview

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  v11.4 IMPLEMENTATION SUMMARY                                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  PHASES (7 total):                                                              │
│  ├── Phase 1: Fix Arc Scopes (P0)                     │ 2 files │ 5 min        │
│  ├── Phase 2: Create shared/config Layer              │ 3 files │ 15 min       │
│  ├── Phase 3: Create Container Nodes                  │ 2 files │ 10 min       │
│  ├── Phase 4: Update SEO Nodes                        │ 2 files │ 20 min       │
│  ├── Phase 5: Update GEO Nodes                        │ 2 files │ 15 min       │
│  ├── Phase 6: Create/Update Arcs                      │ 12 files │ 30 min      │
│  └── Phase 7: Delete Obsolete Files + Validate        │ 4 files │ 10 min       │
│                                                                                 │
│  TOTAL: ~26 files, ~1.5 hours                                                   │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Phase 1: Fix Arc Scopes (P0) ✅ COMPLETED

> **Priority**: P0 (must fix before any other changes)
> **Status**: DONE (see v11.5.1 above)

### 1.1 Update TARGETS Arc

**File**: `packages/core/models/arc-kinds/semantic/targets.yaml`

```yaml
# CHANGE:
scope: intra_realm  # OLD
scope: cross_realm  # NEW (EntityContent [org] → SEOKeyword [shared])
```

**Full change**:
- Line 12: `scope: intra_realm` → `scope: cross_realm`
- Update llm_context to mention cross-realm nature

### 1.2 Update MONITORS_GEO Arc

**File**: `packages/core/models/arc-kinds/semantic/monitors-geo.yaml`

```yaml
# CHANGE:
scope: intra_realm  # OLD
scope: cross_realm  # NEW (EntityContent [org] → GEOQuery [shared])
```

**Full change**:
- Line 14: `scope: intra_realm` → `scope: cross_realm`
- Update llm_context

---

## Phase 2: Create shared/config Layer

> **Priority**: P1 (enables classification nodes)

### 2.1 Create Directory

```bash
mkdir -p packages/core/models/node-kinds/shared/config
```

### 2.2 Move EntityCategory

**From**: `packages/core/models/node-kinds/shared/knowledge/entity-category.yaml`
**To**: `packages/core/models/node-kinds/shared/config/entity-category.yaml`

**Changes**:
- Line 11: `layer: knowledge` → `layer: config`
- Update file header comment

### 2.3 Create SEOKeywordFormat

**File**: `packages/core/models/node-kinds/shared/config/seo-keyword-format.yaml`

```yaml
node:
  name: SEOKeywordFormat
  realm: shared
  layer: config
  trait: invariant
  icon: "🏷️"
  description: "Keyword format classification (EntityCategory pattern, ADR-017)"

  properties:
    format_key:
      type: string
      required: true
      enum: [standard, question, comparison, preposition, long_tail, brand, local]
    display_name:
      type: string
      required: true
    description:
      type: string
    has_entity_links:
      type: boolean
      default: false

  llm_context: |
    SEOKeywordFormat classifies keywords by structural format.
    7 invariant instances: standard, question, comparison, preposition,
    long_tail, brand, local. Use HAS_FORMAT arc from SEOKeyword.
    Formats with has_entity_links=true have COMPARES_A/B or USE_CASE_FOR arcs.
```

**Seed data** (7 instances):

| format_key | display_name | has_entity_links |
|------------|--------------|------------------|
| standard | Standard | false |
| question | Question | false |
| comparison | Comparison | true |
| preposition | Preposition | true |
| long_tail | Long-tail | false |
| brand | Brand | true |
| local | Local | false |

---

## Phase 3: Create Container Nodes

> **Priority**: P1 (enables Knowledge Atoms pattern)

### 3.1 Create SEOKeywordSet

**File**: `packages/core/models/node-kinds/shared/knowledge/seo-keyword-set.yaml`

```yaml
node:
  name: SEOKeywordSet
  realm: shared
  layer: knowledge
  trait: invariant  # Container is universal category
  icon: "📦"
  description: "Container for SEO keywords belonging to a locale (1:1 with Locale)"

  properties:
    last_sync:
      type: datetime
      description: "Last DataForSEO sync timestamp"
    data_source:
      type: string
      default: "dataforseo"
    total_keywords:
      type: int
      description: "Count of keywords in this set"

  llm_context: |
    SEOKeywordSet groups all SEO keywords for a locale.
    One SEOKeywordSet per Locale (1:1 cardinality).
    Access: (Locale)-[:HAS_SEO_KEYWORDS]->(SEOKeywordSet)
    Then: (SEOKeywordSet)-[:CONTAINS_SEO_KEYWORD]->(SEOKeyword)
```

### 3.2 Create GEOQuerySet

**File**: `packages/core/models/node-kinds/shared/knowledge/geo-query-set.yaml`

```yaml
node:
  name: GEOQuerySet
  realm: shared
  layer: knowledge
  trait: invariant  # Container is universal category
  icon: "📦"
  description: "Container for GEO queries belonging to a locale (1:1 with Locale)"

  properties:
    last_sync:
      type: datetime
      description: "Last AI visibility check timestamp"
    total_queries:
      type: int
      description: "Count of queries in this set"

  llm_context: |
    GEOQuerySet groups all GEO queries for a locale.
    One GEOQuerySet per Locale (1:1 cardinality).
    Access: (Locale)-[:HAS_GEO_QUERIES]->(GEOQuerySet)
    Then: (GEOQuerySet)-[:CONTAINS_GEO_QUERY]->(GEOQuery)
```

---

## Phase 4: Update SEO Nodes

> **Priority**: P1 (core schema changes)

### 4.1 Update SEOKeyword

**File**: `packages/core/models/node-kinds/shared/knowledge/seo-keyword.yaml`

**Changes**:

1. **Remove time-series properties** (move to SEOKeywordMetrics):
   - `volume` → `monthly_search_volume`
   - `difficulty` → `ranking_difficulty`
   - `cpc` → `cost_per_click`
   - `traffic_potential`
   - `clicks` → `estimated_clicks`
   - `clicks_per_search`
   - `serp_features`
   - `competition` → `organic_competition`
   - `trend`
   - `seasonality` → `seasonality_pattern` (keep on SEOKeyword as stable characteristic)

2. **Add format-specific properties**:
   - `question_word` (when format=question)
   - `answer_type` (when format=question)
   - `comparison_type` (when format=comparison)
   - `entity_a_key` (when format=comparison)
   - `entity_b_key` (when format=comparison)
   - `preposition` (when format=preposition)
   - `use_case` (when format=preposition)

3. **Add stable characteristics**:
   - `competition_level` (enum: low, medium, high)
   - `categories` (int[] — Google Ads category IDs)

4. **Update relations**:
   - Remove: HAS_QUESTIONS, HAS_COMPARISONS, HAS_PREPOSITIONS
   - Add: HAS_FORMAT → SEOKeywordFormat
   - Add: HAS_SEO_KEYWORD_METRICS → SEOKeywordMetrics
   - Add: COMPARES_A, COMPARES_B → Entity (conditional)
   - Add: USE_CASE_FOR → Entity (conditional)
   - Add: MENTIONS_BRAND → Entity (conditional)

5. **Update llm_context**:
   ```
   SEOKeyword represents market search behavior for a locale.
   It is DISCOVERED data from DataForSEO, not generated content.
   Use to understand what users search for, then TARGET from EntityContent.
   Format determines which optional properties are populated.
   Time-series metrics: follow HAS_SEO_KEYWORD_METRICS arc.
   ```

### 4.2 Update SEOKeywordMetrics

**File**: `packages/core/models/node-kinds/shared/knowledge/seo-keyword-metrics.yaml`

**Changes**:

1. **Verify trait**: `trait: aggregated` (not derived)

2. **Add missing time-series properties**:
   - `observed_at` (required)
   - `source` (required)
   - `observation_period`
   - `monthly_search_volume`
   - `estimated_clicks`
   - `clicks_per_search`
   - `traffic_potential`
   - `ranking_difficulty`
   - `organic_competition`
   - `cost_per_click`
   - `low_top_of_page_bid`
   - `high_top_of_page_bid`
   - `serp_features` (string[])
   - `serp_result_count`
   - `volume_change_7d`
   - `volume_change_30d`

3. **Update llm_context**:
   ```
   SEOKeywordMetrics is a time-series snapshot of keyword performance.
   Query latest: ORDER BY observed_at DESC LIMIT 1
   Query trend: multiple nodes over time for the same keyword.
   Immutable: never update, always append new observations.
   ```

---

## Phase 5: Update GEO Nodes

> **Priority**: P1 (core schema changes)

### 5.1 Update GEOQuery

**File**: `packages/core/models/node-kinds/shared/knowledge/geo-query.yaml`

**Changes**:

1. **Fix realm/layer**:
   - `realm: org` → `realm: shared`
   - `layer: geo` → `layer: knowledge`

2. **Remove time-series properties** (move to GEOAnswer):
   - `gemini_visibility`
   - `gpt_visibility`
   - `perplexity_visibility`
   - `claude_visibility`
   - `ai_visibility_score`
   - `share_of_voice`
   - `citation_frequency`

3. **Keep only static properties**:
   - `value` (required)
   - `query_type` (required)
   - `language_hint`
   - `platforms` (string[])

4. **Update relations**:
   - Remove: HAS_GEO_METRICS (GEOMetrics being deleted)
   - Keep: HAS_GEO_ANSWERS → GEOAnswer (renamed to HAS_GEO_ANSWER singular)

5. **Update llm_context** (from Agent 5 recommendations)

### 5.2 Update GEOAnswer

**File**: `packages/core/models/node-kinds/shared/knowledge/geo-answer.yaml`

**Changes**:

1. **Verify trait**: `trait: aggregated`

2. **Add complete properties** (per spec Section 4b):
   - Observation metadata: `observed_at`, `engine`, `engine_version`, `query_variant`
   - Answer content: `response_text`, `response_length`, `cited_urls`, `cited_sources`
   - Visibility metrics: `mentions_entity`, `is_cited`, `citation_position`, `sentiment`, `visibility_score`
   - Content analysis: `competitor_mentions`, `recommendation_rank`, `is_primary_recommendation`

3. **Update llm_context**

---

## Phase 6: Create/Update Arc Definitions

> **Priority**: P1 (completes schema)

### 6.1 Update Existing Arcs

| File | Change |
|------|--------|
| `has-seo-keywords.yaml` | Target: SEOKeyword → SEOKeywordSet, scope: intra_realm |
| `has-geo-queries.yaml` | Target: GEOQuery → GEOQuerySet, scope: intra_realm |
| `has-geo-answers.yaml` | Rename to `has-geo-answer.yaml` (singular) |

### 6.2 Create New Arcs

| Arc | Source | Target | Family | Scope | Cardinality |
|-----|--------|--------|--------|-------|-------------|
| `CONTAINS_SEO_KEYWORD` | SEOKeywordSet | SEOKeyword | ownership | intra_realm | 1:N |
| `CONTAINS_GEO_QUERY` | GEOQuerySet | GEOQuery | ownership | intra_realm | 1:N |
| `HAS_FORMAT` | SEOKeyword | SEOKeywordFormat | ownership | intra_realm | N:1 |
| `HAS_SEO_KEYWORD_METRICS` | SEOKeyword | SEOKeywordMetrics | ownership | intra_realm | 1:N |
| `COMPARES_A` | SEOKeyword | Entity | semantic | cross_realm | N:1 |
| `COMPARES_B` | SEOKeyword | Entity | semantic | cross_realm | N:1 |
| `USE_CASE_FOR` | SEOKeyword | Entity | semantic | cross_realm | N:1 |
| `MENTIONS_BRAND` | SEOKeyword | Entity | semantic | cross_realm | N:1 |

### 6.3 Delete Obsolete Arcs

| File | Reason |
|------|--------|
| `has-questions.yaml` | SEOQuestion deleted |
| `has-comparisons.yaml` | SEOComparison deleted |
| `has-prepositions.yaml` | SEOPreposition deleted |
| `has-geo-metrics.yaml` | GEOMetrics deleted |

---

## Phase 7: Delete Obsolete Files + Validate

> **Priority**: P1 (cleanup)

### 7.1 Delete Node Files

```bash
rm packages/core/models/node-kinds/shared/knowledge/seo-question.yaml
rm packages/core/models/node-kinds/shared/knowledge/seo-comparison.yaml
rm packages/core/models/node-kinds/shared/knowledge/seo-preposition.yaml
rm packages/core/models/node-kinds/shared/knowledge/geo-metrics.yaml
```

### 7.2 Validate Schema

```bash
cargo run -- schema generate
cargo run -- schema validate --strict
cargo nextest run
```

### 7.3 Update Documentation

- [ ] Update root `CLAUDE.md`
- [ ] Update `tools/novanet/CLAUDE.md`
- [ ] Update `.claude/rules/novanet-decisions.md` (add ADR-018)
- [ ] Update `.claude/rules/novanet-terminology.md`

---

## Execution Checkpoints

### After Phase 1 (Arc Scopes)

```bash
cargo run -- schema validate
# Expect: TARGETS and MONITORS_GEO now cross_realm
```

### After Phase 3 (Containers)

```bash
cargo run -- blueprint --view=tree
# Expect: SEOKeywordSet, GEOQuerySet in shared/knowledge
```

### After Phase 4-5 (Node Updates)

```bash
cargo run -- schema generate
cargo run -- schema validate --strict
# Expect: All SEO/GEO nodes with correct realm/layer/trait
```

### After Phase 6 (Arcs)

```bash
cargo run -- blueprint --view=arcs
# Expect: 14 arcs for SEO/GEO with correct scopes
```

### After Phase 7 (Complete)

```bash
cargo nextest run
pnpm type-check
cargo run -- tui
# Full validation
```

---

## Rollback Plan

If issues found after migration:

1. **Phase 1-3**: Safe to revert (additive changes)
2. **Phase 4-5**: Requires restoring node properties
3. **Phase 6**: Requires restoring arc files from git
4. **Phase 7**: Cannot restore deleted files (ensure backup)

**Before Phase 7, create backup**:
```bash
git stash push packages/core/models/node-kinds/shared/knowledge/seo-*.yaml
git stash push packages/core/models/node-kinds/shared/knowledge/geo-metrics.yaml
```

---

## Success Criteria

- [ ] `cargo run -- schema validate --strict` passes
- [ ] `cargo nextest run` — 0 failures
- [ ] `pnpm type-check` — 0 errors
- [ ] TUI shows correct SEO/GEO nodes in shared/knowledge
- [ ] Blueprint shows 4 layers in shared (config, locale, geography, knowledge)
- [ ] Blueprint shows 6 layers in org (config, semantic, foundation, structure, instruction, output)
- [ ] All 8 cross-realm arcs validated (per Agent 4 checklist)
