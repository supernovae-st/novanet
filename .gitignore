# Dependencies
node_modules/

# Build outputs
dist/
.next/
.turbo/

# Environment
.env
.env.local
.env*.local

# MCP Configuration (may contain secrets)
.mcp.json

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude Code - PRIVATE DX (symlinked to dx/.claude/novanet/)
# All DX content is in private supernovae-agi/dx/ repo
# Note: No trailing slash to match symlinks
.claude

# Only CLAUDE.md is public (project context for contributors)
!CLAUDE.md

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Test
coverage/

# Rust
tools/novanet/target/

# mdBook generated output
docs/book/

# Docker
*.pid
novanet-public-repo/
.worktrees/

# Database backups (use pnpm db:backup:list to see them)
backups/

# npm (we use pnpm)
package-lock.json

# Python cache
__pycache__/

# Proptest regressions
proptest-regressions/
tools/target/
# DX symlink (no trailing slash to match symlink)
/dx

# === PRIVATE DATA (in supernovae-agi/brain/) ===
# All QR Code AI specific content is in private brain/ repo
brain/

# Private seed data (QR Code AI entities, content, SEO)
# content/ is a symlink to private-data/seed/content/
packages/db/seed/content
packages/db/seed/10-*
packages/db/seed/11-*
packages/db/seed/3[1-9]-*
packages/db/seed/4*
packages/db/seed/5*
packages/db/seed/migration-*

# Private SEO/research data (symlinks to brain/)
# No trailing slash to match symlinks
docs/assets/keywords
docs/assets/keywods
research/seo_*
scripts/seo-import/output

# Private work sessions (symlink to brain/)
docs/sessions
backup-*.cypher
